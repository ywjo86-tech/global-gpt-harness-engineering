# Orchestrator Outputs

This directory stores generated artifacts for the local orchestration runtime.

- Run packages are written under project-local `runtime/orchestrator_runs/{run_id}/`.
- This directory exists only as a repository-local reference point for the outputs contract.
- No secrets or remote artifacts should be stored here.

