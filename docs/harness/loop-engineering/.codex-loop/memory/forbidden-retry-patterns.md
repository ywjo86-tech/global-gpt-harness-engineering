# 재사용 금지 패턴

## 목적

이 파일은 이미 실패한 접근 방식을 저장하며, 같은 실패 맥락에서 다시 쓰지 않기 위해 존재한다.

## 기록 형식

| 패턴 ID | 파일 또는 영역 | 실패 원인 | 실패한 접근 | 대체 접근 |
|---|---|---|---|---|

## 규칙

- 새 루프를 시작하기 전에 이 파일을 확인한다.
- 같은 파일 + 같은 원인 + 같은 방식 조합은 재사용하지 않는다.
- 재사용 가치가 있는 실패가 나오면 새 항목을 추가한다.
